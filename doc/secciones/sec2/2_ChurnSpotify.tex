\section{Churn de usuarios en Spotify}

% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %

\subsection{Descripción del proyecto}

\subsubsection{Objetivo}

El objetivo es predecir la pérdida de usuarios (\textit{churn} en inglés) en función de datos personales de los usuarios y de su interacción con el servicio. Las conclusiones de este análisis intentan ayudar en la toma de decisiones de la empresa para reducir el número de cancelaciones.

% ---------------------------------------------------------------------------------------------------------- %

\subsubsection{Datos}

El conjunto de datos que usamos para entrenar nuestro modelo está disponible en la base de datos de Kaggle: \href{https://www.kaggle.com/datasets/nabihazahid/spotify-dataset-for-churn-analysis/data}{Spotify Analysis Dataset 2025}. 
Ya hemos descargado el fichero con extensión \texttt{.csv} en el directorio \texttt{datasets}.

En el \textit{dataset} encontramos 12 columnas, donde diferenciamos a los atributos de la variable objetivo. 
La variable objetivo, o \textit{target}, es aquella que deseamos predecir a partir de los atributos; 
en este caso esta es \code{is\_churned}, y es una variable binaria que toma dos valores:
\begin{itemize}
    \item \code{1}, cuando el usuario ha cancelado su cuenta; o
    \item \code{0}, cuando el usuario sigue activo.
\end{itemize}

En los atributos podemos diferenciar dos principales tipos de variables: numéricas y categóricas. 
Dentro de las variables numéricas, se pueden distinguir aquellas representadas como números enteros y las que se expresan en formato en coma flotante.

Cada tipo de dato requiere un tratamiento específico, y algunos modelos serán más adecuados que otros para cada tipo. En ocasiones, será necesario modificar la codificación para garantizar una mejor compatibilidad con el modelo.
\begin{itemize}

    \item Modelos como los de árboles de decisión, \textit{Random Forest} o \textit{Naive Bayes} ---en su forma base--- funcionan con datos categóricos.
    
    \item Modelos como los de \textit{K-Nearest Neighbour}, las \textit{Support Vector Machine} o las redes neuronales, ---de nuevo, hablamos en su forma base--- trabajan con datos numéricos.

\end{itemize}

Además, las variables categóricas y numéricas presentan una semántica muy distinta. Generalmente, las variables categóricas no presentan un orden inherente en sus valores, a diferencia de las variables numéricas. Sin embargo, es común encontrar en ciertos conjuntos de datos variables codificadas con números enteros que actúan como códigos, pero que, desde un punto de vista semántico, se comportan como variables categóricas, ya que no presentan un orden inherente en sus valores; por ejemplo, el código \code{2} no está más cerca del \code{3} que del \code{4}. Cuando identifiquemos este tipo de variables, el primer paso en el preprocesamiento será cambiar su codificación para evitar que los modelos los interpreten como variables numéricas, que puedan llegar a tener alterar el sentido de una predicción.

El caso opuesto también puede darse cuando encontramos variables categóricas que presentan un orden lineal en su significado, aunque este no esté reflejado en su valor de texto. 
A estas variables, les asignaremos valores numéricos para capturar ese orden en el análisis.

En este proyecto, encontramos una variable de tipo categórico representada por números enteros, esta es la variable objetivo \code{is\_churned}, anteriormente analizada. 
En la primera modificación del conjunto de datos original, cambiaremos el tipo de dato de esta columna a \textit{string}, reemplazando el valor \code{1} por \code{CHURNED}, y el \code{0} por \code{NOT\_CHURNED}.

El resto de variables del dataset son las siguientes:

    \begin{itemize}
        \item \code{user\_id}: De tipo entero. Identifica a cada usuario.
        \item \code{gender}: De tipo categórico (\textit{string}). Genéro del usuario.
        \item \code{age}: De tipo entero. Edad del usuario.
        \item \code{country}: De tipo categórico (\textit{string}). País del usuario. 
        \item \code{subscription\_type}: De tipo categórico (\textit{string}). Tipo de suscripción del usuario.
        \item \code{listening\_time}: De tipo entero. Número de minutos por día reproduciendo, en poromedio. 
        \item \code{songs\_played\_per\_day}: De tipo entero. Número de canciones reproducidas diariamente, en promedio.
        \item \code{skip\_rate}: De tipo decimal (\textit{float}). Porcentaje de canciones saltadas. 
        \item \code{device\_type}: De tipo categórico (\textit{string}). Tipo de dispositivo usado. 
        \item \code{ads\_listened\_per\_week}: De tipo entero. Número de anuncios reproducidos por semana.
        \item \code{offline\_listening}: De tipo entero, aunque semánticamente booleano. Uso del modo \textit{offline}. 
    \end{itemize}

% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %

\subsection{Exploración y preprocesado de datos}

\subsubsection{Carga de datos}

La carga de datos la realizamos con un nodo de entrada <<CSV Reader>>. En la Figura \ref{fig:spotify_churn_CSV_reader} vemos que el conjunto de datos tiene 8 000 instancias, cada una con 12 características y su \textbf{etiqueta} (el valor real de la variable \textit{target} en cada instancia).

\begin{figure}[h!]
    \fbox{\includegraphics[width=1.0\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_CSV_reader.png}}
    \caption{
        Visualización de los datos crudos (\textit{raw data}, en inglés) del primer problema.
        Obtenido del nodo <<CSV Reader>>. 
    }
    \label{fig:spotify_churn_CSV_reader}
\end{figure}

En las siguientes subsecciones preparamos los datos, para facilitar su interpretación y exploración inicial. Y es que, muchas veces, los datos pueden estar incompletos, contener valores atípicos o estar en formatos difíciles de analizar directamente. Posteriormente, realizaremos ajustes específicos en función de los requisitos de cada modelo.

% ---------------------------------------------------------------------------------------------------------- %

\subsubsection{Análisis de variables}

Ahora realizaremos un pequeño análisis tanto de las características como de la variable objetivo. Comenzaremos utilizando el nodo <<Data Explorer>> de la extensión \textit{KNIME JavaScript Views (Labs)}, tanto para las variables categóricas como las numéricas.

\paragraph*{Variables categóricas.}

La variable objetivo ha sido convertida a una variable categórica (\code{churned\_status}), para poder analizarse como tal. En la Figura \ref{fig:spotify_churn_statistics_categorical_variables} podemos ver los resultados del análisis exploratorio sobre cada variable categórica. 

\begin{figure}[h!]
    \centering
    \fbox{\includegraphics[width=0.85\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_statistics_categorical_variables.png}}
    \caption{Visualización del análisis exploratorio de variables categóricas. 
    Obtenido del nodo <<Data Explorer>> de la extensión \textit{KNIME JavaScript Views (Labs)}.}
    \label{fig:spotify_churn_statistics_categorical_variables}
\end{figure}

Analicemos la información extraída de este reporte:

\begin{itemize}
    \item \code{gender} tiene tres valores posibles: \code{Male}, \code{Female} y \code{Other}. La frecuencia de los tres grupos es aparentemente idéntica. 
    
    \item \code{country} tiene ocho valores posibles: \code{AU} (Australia), \code{US} (Estados Unidos), \code{DE} (Dinamarca), \code{IN} (India), \code{PK} (Pakistán), \code{FR} (Francia), \code{UK} (Reino Unido) y \code{CA} (Canadá). La frecuencia de todos los grupos es muy similar, 
    
    \item \code{subscription\_type} tiene cuatro valores posibles: \code{Free}, \code{Premium}, \code{Student} y \code{Family}. 
    
    \item \code{device\_type} tiene tres valores posibles: \code{Desktop}, \code{Web} y \code{Mobile}.

\end{itemize}

En todas las características comprobamos que las diversas categorías están muy equilibradas. La mayor diferencia de ocurrencias entre dos categorías de cada variable son de apenas un 0.5\% en \code{gender}, 1\% en \code{country}, 2.4\% en \code{subscription\_type} y 2.3\% en \code{device\_type}. La variable objetivo sí presenta dos clases desbalanceadas: un 74\% de las instancias son de usuarios que no han cancelado su cuenta, dejando solo un 26\% de las instancias positivas. 

Por otro lado, la variable objetivo, \code{churned\_status}, presenta \code{CHURNED} en el 25.89\% de las instancias, y \code{NOT\_CHURNED} en el 74.11\% restante. 

¿Y cómo se relacionan los atributos con ella?
En la Figura \ref{fig:churn_spotify_parallelcategories_categorical_attributes} se muestra cómo se corresponden estas variables categóricas en sus distintos valores con las clases de la variable objetivo mediante un gráfico de Categorías Paralelas. Este gráfico es súmamente útil, ya que nos permite ver qué proporción de cada valor hay por cada variable categórica, identificar combinaciones más comunes entre categorías, así como ver la proporción de préstamos aprobados o denegados por cada uno de esos valores. En la gráfica se observa que existe prácticamente la misma proporción de usuarios dentro de cada grupo que cancela la cuenta, es decir, el \textit{churn} se distribuye de manera uniforme entre las diferentes categorías analizadas, sin que ninguna combinación particular de atributos categóricos muestre una predisposición significativamente mayor a la cancelación del servicio. Esto sugiere que estas variables categóricas por sí solas no son predictores fuertes de la cancelación.

\begin{figure}[h!]
    \fbox{\includegraphics[width=1.0\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_parallelcategories_categorical_variables.png}}
    \caption{Visualización de categorías paralelas en las variables categóricas.}
    \label{fig:churn_spotify_parallelcategories_categorical_attributes}
\end{figure} 

\begin{tcolorbox}
    [colback=gray!5!white, colframe=gray!60!black, title=Asociación entre dos variables categóricas, breakable]

    Cuando las variables son categóricas, no se puede calcular una correlación tradicional (como la de Pearson). En su lugar, se mide la asociación mediant el test chi-cuadrado para evaluar si existen dependencia estadística entre las categorías, y luego se normaliza con el V de Cramer para obtener una medida comparable entre 0 y 1.

\end{tcolorbox}

Esto coincide con el análisis de asociación presentado en la Figura \ref{fig:churn_spotify_corrmatrix_categorical}, en el que se muestran unos valores de asociación entre variables prácticamente nulos, tanto entre atributos como entre estos con la variable objetivo.

\begin{figure}[h!]
    \centering
    \fbox{\includegraphics[width=0.8\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_corrmatrix_categorical.png}}
    \caption{
        Matriz de correlación (basada en el valor chi-cuadrado de Pearson, normalizado mediante el coeficiente V de Cramer) entre variables categóricas. Obtenido de un componente creado propio, <<Correlation Matrix>>, que calcula las correlaciones entre cada par de variables (mediante el nodo <<Linear Correlation>>) y las plasma en un mapa de calor.
    }
    \label{fig:churn_spotify_corrmatrix_categorical}
\end{figure} 

% ---------------------------------------------------------------------------------------------------------- %

\paragraph*{Variables numéricas.}

%Primero, analizamos cada variable numérica individualmente. ¿Qué valores abarca? ¿Media/Mediana?
% Comentar la variable offline_listening que es binaria

En la Figura \ref{fig:churn_spotify_statistical_numerical_variables} tenemos un cuadro resumen de las estadísticas principales de las variables numéricas. Algunas conclusiones que podemos obtener de este son: 

\begin{itemize}

    \item No hay valores faltantes en ninguna de las variables.
    
    \item Los valores numéricos se distribuyen de manera muy homogénea en las variables \code{age}, \code{listening\_time}, \code{songs\_played\_per\_day} y \code{skip\_rate}, por lo que los valores están bien dispersos a lo largo de sus rangos respectivos y no hay valores anómalos.
    
    \item La variable \code{ads\_listened\_per\_week} tiene la mayoría de los valores (5982 instancias) cero, dado que la mayoría de cuentas tienen algún plan \textit{premium} y no tienen anuncios. El resto de instancias están diseminadas entre los 1 y 49 anuncios de manera residual.
    
    \item La variable \code{office\_listening}, como veníamos anticipando antes, es realmente binaria, por lo que solo tiene valores 0 y 1, y hay muchos más valores positivos que nulos, por la misma razón: todos los usuarios \textit{premium} tienen acceso al modo \textit{offline}. De hecho, si se suman las instancias con valor nulo de \code{ads\_listened\_per\_week} y \code{office\_listening} da 8 000 instancias, que es el total, lo que indica que estas dos variables están completamente alineadas: los usuarios que no escuchan anuncios son exactamente los que pueden usar el modo \textit{offline}, y viceversa.

\end{itemize}


\begin{figure}[h!]
    \fbox{\includegraphics[width=1.0\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_statistics_numerical_variables.png}}
    \caption{
        Visualización del análisis exploratorio de variables numéricas.
        Obtenido del nodo <<Data Explorer>> de la extensión \textit{KNIME JavaScript Views (Labs)}.
    }
    \label{fig:churn_spotify_statistical_numerical_variables}
\end{figure} 


Para comprobar si se relacionan entre sí las variables numéricas, nos apoyaremos de la matriz de correlación de la Figura \ref{fig:churn_spotify_corrmatrix_numerical}. Como se puede observar, las únicas correlaciones significativas la encontramos entre \code{offline\_listening} y \code{ads\_listened\_per\_week}, que se correlacionan inversamente, como veníamos indicando anteriormente. La correlación no es de $-1$ exacto, ya que la variable \code{offline\_listening} es binaria y no captura completamente la variabilidad del número de anuncios escuchados, además de que pueden existir otros factores que influyan en el número de anuncios mostrados.

\begin{figure}[h!]
    \centering
    \fbox{\includegraphics[width=0.8\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_corrmatrix_numerical.png}}
    \caption{
        Matriz de correlación entre variables numéricas (basada en el coeficiente de correlación de Pearson). 
        Obtenido de un componente creado propio, <<Correlation Matrix>>, que calcula las correlaciones entre cada par de variables (mediante el nodo <<Linear Correlation>>) y las plasma en un mapa de calor. 
    }
    \label{fig:churn_spotify_corrmatrix_numerical}
\end{figure} 


% \begin{tcolorbox}
%     [colback=gray!5!white, colframe=gray!60!black, title=Medida de asociación, breakable]

%     Para analizar la asociación entre una variable numérica y una variable categórica se realiza comparación de distribuciones de la variable numérica entre los diferentes grupos definidos por la variable categórica.

%     Hay varias pruebas estadísticas:
%     \begin{itemize}
%         \item \textbf{Pruebas paramétricas}: Estas pruebas asumen que la variable numérica sigue una distribución específica (generalmente la normal).
        
%         Las siguientes pruebas asumen que la variable numérica sigue una distribución normal en cada grupo y que las varianzas de los grupos son aproximadamente iguales (homocedasticidad).

%         \begin{itemize}
%             \item Prueba $t$ de Student: Para dos grupos. ...
%             \item Análisis de varianza (ANOVA): Para tres o más grupos. ...
%         \end{itemize}

%         También encontramos pruebas diseñadas para casos en los que la variable numérica presenta normalidad pero no homocedasticidad:

%         \begin{itemize}
%             \item Prueba $t$ de Welch: Para dos grupos. ...
%             \item Pruba Welch ANOVA: Para tres o más grupos. ...
%         \end{itemize}
        
%         \item \textbf{Pruebas no parámetricas}: Estas pruebas se utilizan en casos en los que las variables no se ajustan a modelos teóricos. Estas pruebas son menos efectivas que las parámetricas para distribuciones normales, pero en los casos en los que no se cumplen los supuestos paramétricos (como normalidad), son más robustas y fiables. Se basan en los rangos de los datos.
        
%         Algunas de estas pruebas son:
        
%         \begin{itemize}
%             \item Prueba $U$ de Wilcoxon-Mann-Whitney: Alternativa a la prueba $t$ de Student que compara las medianas o distribuciones de los rangos de dos grupos independientes.
%             \item Prueba de Kruskal-Wallis: Alternativa al ANOVA que compara las medianas o distribuciones de los rangos de tres o más grupos independientes.
%         \end{itemize}
        
%         Cuando se comparan tres o más grupos, si la prueba ANOVA, Welch ANOVA o de Kruskal-Wallis resulta significativa, generalmente se requieren pruebas \textit{post-hoc} (como la prueba de Tukey para ANOVA, prueba \textit{post-hoc} de Games-Howell comparaciones por pares de Mann-Whitney con corrección de Bonferroni) para determinar cuáles grupos difieren entre sí.
        
%     \end{itemize}
    
% \end{tcolorbox}

Por último, en la Figura \ref{fig:churn_spotify_U_Wilcoxon-Mann-Whitney_numerical} analizamos cómo se relacionan estas variables numéricas con la variable objetivo (categórica). Para ello, comparamos la distribución de las instancias pertenecientes a cada categoría de la variable objetivo mediante la prueba $U$ de Wilcoxon-Mann-Whitney. Se ha escogido esta prueba no paramétrica ya que la distribución de las variables en ningún caso parece normal: las variables \code{age}, \code{listening\_time}, \code{songs\_played\_per\_day} y \code{skip\_rate} parecen tener una distribución uniforme; \code{ads\_listened\_per\_week} un conteo disperso, en el que la mayoría de valores son cero y el resto están dispersos entre 1 y 49; y \code{offline\_listening} es una variable binaria, que obviamente solo presenta valores 0 y 1. Se observa que ---para un nivel de confianza del 95\%--- en ningún caso se puede rechazar que las instancias de ambas categorías presenten la misma distrución (\textit{p-value} $> 0.05$). Por tanto, estos atributos \textit{per se} tampoco parecen ayudar a disinguir la clase. 

\begin{figure}[h!]
    \centering
    \fbox{\includegraphics[width=\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_mann_whitney_numerical.png}}
    \caption{
        Prueba $U$ de Wilcoxon-Mann-Whitney aplicada sobre los valores de los atributos numéricos pertenecientes a las instancias de cada categoría. 
    }
    \label{fig:churn_spotify_U_Wilcoxon-Mann-Whitney_numerical}
\end{figure} 

% ---------------------------------------------------------------------------------------------------------- %

\subsubsection{Codificación de los atributos categóricos}

Se propone la codificación \textbf{\textit{One-Hot}} para los atributos categóricos. Esta técnica de codificación transforma las variables categóricas en un formato binario donde cada valor único se transforma en una columna binaria (0 o 1), donde una columna indica la presencia del valor de la categoría en un registro determinado.

De esta forma, las variables categóricas se convierten en numéricas sin una pérdida relevante de información. El principal inconveniente de esta técnica es el aumento de la dimensionalidad del conjunto de datos, especialmente cuando las variables poseen un número elevado de categorías.

En este caso particular, las 4 columnas originales se expanden a 18. No obstante, este incremento de dimensionalidad no supone un impacto considerable en la complejidad computacional del modelo, como se verá más adelante.

% ---------------------------------------------------------------------------------------------------------- %

\subsubsection{Normalización de las variables numéricas}

Finalmente, aplicará normalización \textbf{Z-Score o estandarización} sobre todas las variables. Esta normalización reescala los valores de cada variable de manera que tengan una media igual a 0 y una desviación estándar igual a 1. 

Para un valor $x_i$ de una variable $X$, el Z-Score se calcula como:

$$
z_i = \frac{x_i-\mu}{\sigma}
$$

donde:
\begin{itemize}
    \item $\mu$ es la media de la variable $X$
    \item $\sigma$ es la desviación estándar de $X$
\end{itemize}

% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %

\subsection{Materiales y métodos}

\subsubsection{Modelos escogidos para la comparación}

Se han escogido los siguientes modelos el entrenamiento:

\begin{enumerate}

    \item \textbf{Modelo \textit{k}-nearest neighbors (\textit{k}-NN)}: Este modelo clasifica una instancia en función de las $k$ instancias etiquetadas más cercanas en un espacio de $n$ dimensiones (donde $n$ representa el número de características). La cercanía entre instancias se determina a través de una función de distancia, que puede ser adaptada para manejar distancias incluso entre valores categóricos ---de haberlos, porque en nuestro caso los hemos convertido todos a numéricos--- (si bien, KNIME no permite modificar la función de distancia euclidiana), o asignar pesos diferentes a ciertas características en función de su importancia. También se puede obtener la clasificación ponderando por la distancia a cada una de las instancias más cercanas, lo que significa que las instancias más cercanas tienen mayor influencia en la predicción que las más lejanas.

    Sin embargo, en su versión básica, el modelo trabaja principalmente con variables numéricas, utiliza la distancia Euclidiana entre instancias y requiere normalizar previamente las variables. La normalización es esencial para evitar que características con escalas mayores dominen la distancia, garantizando así que cada variable contribuya de manera equitativa en el cálculo de la cercanía.

    Tiene como principal ventaja que es un modelo bastante intuitivo de entender, y es medianamente explicable ---ya que la predicción se realiza en base a casos similares de los que sí conocemos la clase---. Sin embargo, una desventaja importante del modelo es que puede resultar costoso computacionalmente y lento, ya que para predecir en conjuntos de datos grandes necesita calcular la distancia entre la nueva instancia y todas las instancias etiquetadas en el conjunto de entrenamiento, si bien existen alternativas que permiten optimizar este proceso, como el uso de estructuras de datos especializadas, que reducen el tiempo de búsqueda de los vecinos más cercanos. 

    % ------------------------------------------------------------------------------------------------------ %

    \item \textbf{Modelo Support Vector Machine (VSM)}: Este modelo trata de encontrar un hiperplano óptimo para la separación de dos clases distintas de datos, una a cada lado del hiperplano.
    
    Esta optimización se basa en maximizar el margen entre estas clases, donde el margen se define como la máxima distancia perpendicular entre el hiperplano y los puntos de datos más cercanos de cada clase. Este proceso busca encontrar un equilibrio entre maximizar el margen y minimizar las clasificaciones erróneas.

    Para problemas donde los datos no son linealmente separables ---los que son la mayoría mínimamente complejos---,  se aplica una función kernel. Esta realiza una transformación matemática de los datos originales de forma que se proyecten en un espacio de mayor dimensionalidad, donde las dos clases sí se pueden separar de manera lineal mediante un hiperplano. Esta transformación matemática puede ser lineal, polinómica, radial (gaussiana), o sigmoide, dependiendo del tipo de \textit{kernel} utilizado. Cada tipo de \textit{kernel} aplica una transformación específica para capturar patrones complejos en los datos. Los más empleados son:

    \begin{itemize}

        \item El \textit{kernel} lineal no realiza ninguna transformación adicional y es útil cuando los 
        datos son casi linealmente separables en el espacio original.

        \item El \textit{kernel} polinómico expande el espacio de características usando combinaciones 
        polinómicas de los datos originales, lo que ayuda a capturar relaciones no lineales de 
        grado determinado.
        
    \end{itemize}

    Además, el \textit{kernel} polinómico tendrán un determinado grado, que determinen el poder de separación del \textit{kernel} en espacios complejos. Escoger el grado adecuado en el \textit{kernel} de una SVM es crucial, ya que afecta directamente la capacidad del modelo para generalizar en nuevos datos y evitar el sobreajuste:

    \begin{itemize}

        \item Un bajo grado de un \textit{kernel} polinómico puede hacer que el modelo sea demasiado simple y no capture patrones importantes en los datos.

        \item Un alto grado de un \textit{kernel} polinómico puede hacer que el modelo se ajuste demasiado a los datos de entrenamiento.

    \end{itemize}

    Sin embargo, aumentar mucho la dimensionalidad de los datos lleva al riesgo de sobreajuste, por lo que es necesario controlar la complejidad del modelo mediante parámetros de regularización. En este sentido, existe el hiperparámetro de regularización $C$, que representa el coste de error de las clasificaciones incorrectas, de forma que:

    \begin{itemize}

        \item un valor alto de $C$ resulta en bajo sesgo y alta varianza, ya que los parámetros de la función de decisión se ajustan más a los datos de entrenamiento, lo que puede llevar al sobreajuste; y, por el contrario,
        
        \item un valor bajo de $C$ resulta en alto sesgo y baja varianza, lo que significa que se permite un margen más amplio de clasificaciones incorrectas, lo que puede resultar en un subajuste del modelo.
        
    \end{itemize}

    También cabe comentar que el modelo presenta muy baja explicabilidad, especialmente cuando se aplican \textit{kernels} no lineales, donde las fronteras de decisión no son fácilmente interpretables debido a la transformación compleja del espacio de características.

    % ------------------------------------------------------------------------------------------------------ %

    \item \textbf{Decision Tree}: El árbol de decisión en problemas de clasificación funciona como un modelo de toma de decisiones estructurado jerárquicamente, donde cada nodo interno representa una pregunta basada en las características de los datos, cada rama representa una posible respuesta o criterio de decisión, y cada hoja representa la predicción final de la clase. Cada hoja está asociada a una clase específica.

    Este modelo es de los más explicables, ya que permite visualizar cómo se toma cada decisión al seguir un camino específico en el árbol. Esto se pude ver en la Figura \ref{fig:churn_spotify_decisiontree}, en la que anticipamos el árbol de decisión obtenido en el posterior apartado. 

    \begin{figure}[h!]
        \centering
        \fbox{\includegraphics[width=0.8\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_decision_tree.png}}
        \caption{Extracto de un árbol de decisión.}
        \label{fig:churn_spotify_decisiontree}
    \end{figure} 

    Los criterios de decisión en cada nodo son simples y se pueden interpretar directamente en términos de las características de los datos, lo cual facilita la comprensión del proceso de clasificación.

    Ahora bien, ¿cómo se crea este árbol de decisión? Existen muy diversos algoritmos. En la descripción del nodo <<Tree Learner>> se indica que la mayor parte de la implementación corresponde al algoritmo C4.5 \cite{quinlan1993program}, estudiado en clase. Este algoritmo era una mejora significativa del algoritmo ID3 \cite{quinlan1986induction}, permitiendo trabajar con atributos numéricos continuos, como en los de nuestro dataset. Como se indica en la propia descripción, C4.5 evalúa posibles umbrales y selecciona el valor que maximiza la ganancia de información, convirtiendo así los valores continuos en binarios.

    Para seleccionar el atributo que mejor divide los datos en cada nodo del árbol se utilizará el índice de Gini, una métrica que mide la impureza en los árboles de decisión.

    \begin{tcolorbox}
        [colback=gray!5!white, colframe=gray!60!black, title=Índice de Gini, breakable]
        {\setlength{\parskip}{0.5em plus 0.2em minus 0.1em}

        La impureza mide cuán mezcladas están las clases en un conjunto de datos. Un conjunto puro (valor de índice bajo) tiene instancias que pertenecen a una sola clase, mientras que un conjunto impuro (valor de índice alto) contiene instancias de múltiples clases.

        El índice de Gini se define como:

        $$
        \textnormal{Gini} = 1 - \sum_{i=1}^C p_i^2
        $$
        donde $p_i$ es la proporción de instancias pertenecientes a la clase $i$ dentro del nodo y $C$ es el número total de clases. 

        El valor del índice de Gini oscila entre 0 y 0.5 en problemas binarios: un valor de 0 indica pureza total(todas las observaciones pertenecen a una sola clase), mientras que valores más altos reflejan mayor mezcla de clases.

        Durante la construcción del árbol, el algoritmo busca dividir los datos de forma que el índice de Gini resultante en los nodos hijos sea lo más bajo posible, es decir, que las particiones sean lo más puras posible.
        }
    \end{tcolorbox}

    El comportamiento y la complejidad del árbol de decisión pueden controlarse mediante diversos hiperparámetros, entre los que destacan:

    \begin{itemize}

        \item Profundidad máxima del árbol: Limitar este valor ayuda a prevenir el sobreajuste, evitando que el árbol memorice el conjunto de entrenamiento.
        
        \item Número mínimo de instancias necesarias para dividir un nodo interno: Si la cantidad de instancias en un nodo interno es menor a este número, entonces el nodo se convertirá en un nodo hoja. Un valor mayor genera árboles más generales y menos propensos al sobreajuste.
        
        \item Número mínimo de instancias requeridas para ser nodo hoja: Una división solo puede ocurrir si deja al menos este número de instancias en los dos nodos resultantes.
        Controla el tamaño mínimo de los nodos terminales y suaviza las predicciones.
        
    \end{itemize}

    El ajuste adecuado de estos hiperparámetros permite equilibrar la interpretabilidad del modelo y su capacidad predictiva, logrando un árbol suficientemente expresivo pero sin sobreajustarse a los datos. Lamentablemente, el nodo <<Decision Tree Learner>> de KNIME solo permite modificar el número mínimo de instancias necesarias para dividir un nodo. 

    Durante la construcción del árbol, se busca dividir los datos hasta alcanzar una partición lo más pura posible. Sin embargo, si se permite que el árbol crezca sin restricciones, este puede llegar a sobreajustarse, es decir, adaptarse demasiado a las particularidades del conjunto de entrenamiento y perder capacidad de generalización. Para evitarlo, se emplea una técnica conocida como \textbf{poda} (\textit{pruning}).

    La poda consiste en reducir el tamaño del árbol eliminando ramas que aportan poca o ninguna mejora significativa en la precisión del modelo. El algoritmo C4.5 da la posibilidad de usar la poda MDL (\textit{Minimum Description Length}), que aplica una heurística para reducir el tamaño del árbol y mejorar su generalización. Este proceso contribuye a mejorar la capacidad de generalización del modelo y a mantener una estructura más sencilla y comprensible.

    % ------------------------------------------------------------------------------------------------------ %

    \item \textbf{Random Forest}: Propone un algoritmo de tipo \textit{ensemble} que combina múltiples árboles de decisión para mejorar la precisión y robustez de las predicciones. El modelo entrena múltiples árboles de decisión \textit{débiles}. Los \textit{weak models} son modelos de aprendizaje automático que, por sí solos, tienen un desempeño apenas mejor que un modelo que adivina al azar, es decir, que tienen una precisión baja pero que aún pueden identificar patrones en los datos. La filosofía de los \textit{ensemble} es aprovechar la diversidad y la sabiduría colectiva de múltiples modelos para construir un predictor robusto y menos propenso a errores individuales.

    Una de las principales ventajas que tiene el árbol de decisión la pierde el \textit{Random Forest}, que es la explicabilidad, dado que al combinar muchos árboles de decisión, el modelo se vuelve más complejo y menos transparente. En un modelo de \textit{Random Forest}, aunque cada árbol es interpretable por separado, la combinación de múltiples árboles hace que el proceso de toma de decisiones sea difícil de seguir y explicar en términos sencillos. A cambio, la robustez del modelo frente al sobreajuste mejora, ya que aunque los árboles individuales pueden sobreajustarse, la combinación de múltiples árboles ayuda a suavizar las predicciones y minimizar este problema. Además, como el entrenamiento de cada árbol es independiente, el algoritmo es paralelizable, siendo adecuado para grandes conjuntos de datos.

    Los principales hiperparámetros de este modelo son los previos mencionados para los árboles de decisión, además del número de árboles que constituirán el ``bosque''. El nodo <<Random Forest Learner>> en KNIME, en este caso, sí nos permite controlar tanto el número de árboles, como la profundidad máxima del árbol (el número de niveles máximo), y el tamaño mínimo del nodo (número de instancias mínimas). 

    % ------------------------------------------------------------------------------------------------------ %

    \item \textbf{Gradient Boosting}: Otro modelo de tipo \textit{ensemble}, que se basa en la construcción secuencial de árboles de decisión para mejorar la precisión del modelo. A diferencia del \textit{Random Forest}, donde los árboles son independientes y se entrenan en paralelo, el \textit{Gradient Boosting} construye cada árbol de manera secuencial, de modo que cada nuevo árbol se entrena para corregir los errores de predicción del conjunto de árboles anterior.

    La idea central detrás de \textit{Gradient Boosting} es minimiza el error en cada etapa del entrenamiento. Este método comienza con un modelo simple, y añade modelos adicionales en función del gradiente de la pérdida, es decir, cada nuevo árbol intenta ajustar los residuos (errores) que los árboles anteriores no lograron predecir correctamente. Este enfoque permite que el modelo se enfoque en las muestras más difíciles o con errores más grandes en cada iteración, lo cual puede resultar en un modelo más preciso y efectivo. Sin embargo, este proceso de ajuste secuencial tiene desventajas en cuanto a eficiencia, ya que los árboles no se pueden entrenar en paralelo, y el modelo puede ser más propenso al sobreajuste si se construyen demasiados árboles o si el ajuste es demasiado específico.

    En términos de interpretabilidad, el \textit{Gradient Boosting} es similar al \textit{Random Forest}: aunque cada árbol individual es interpretable, el modelo completo se vuelve menos transparente debido a la gran cantidad de árboles. En la práctica, \textit{Gradient Boosting} ha demostrado robustez y un rendimiento destacados para múltiples problemas de clasificación, especialmente en problemas con datos tabulares.

\end{enumerate}

% ---------------------------------------------------------------------------------------------------------- %

\subsubsection{Métricas escogidas para la evaluación del rendimiento}

\begin{itemize}

    \item \textbf{Matriz de confusión}: Esta herramienta permite visualizar el desempeño de un algoritmo de clasificación binaria. Cada columna de la matriz representa el número de predicciones de cada clase, mientras que cada fila representa a las instancias en la clase real, de forma que podemos permite observar cómo se distribuyen las predicciones del modelo respecto a las clases verdaderas.

    En los problema de clasificación binaria las matrices de confusión tienen una estructura como la mostrada en \ref{fig:churn_spotify_confusion_matrix_binary}. En esta estructura encontramos:

    \begin{itemize}
        \item $TP$ es el número de verdaderos positivos (instancias positivas correctamente clasificadas).
        \item $TN$ es el número de verdaderos negativos (instancias negativas correctamente clasificadas).
        \item $FP$ es el número de falsos positivos (instancias negativas incorrectamente clasificadas como positivas).
        \item $FN$ es el número de falsos negativos (instancias positivas incorrectamente clasificadas como negativas).
    \end{itemize}

    \begin{figure}[h!]
        \centering
        \fbox{\includegraphics[width=0.5\textwidth, keepaspectratio]{secciones/sec2/imagenes/confusion_matrix_binary.png}}
        \caption{Estructura de una matriz de confusión en clasificación binaria.}
        \label{fig:churn_spotify_confusion_matrix_binary}
    \end{figure}

    Muchas de las siguientes métricas son calculadas a partir de los datos de esta matriz de confusión.

    % ------------------------------------------------------------------------------------------------------ %
    
    \item \textbf{Exactitud (\textit{accuracy})}: Es la proporción de predicciones correctas respecto al total de predicciones realizadas. Es la más básica de todas las métricas.

    $$
    Accuracy = \frac{TP+TN}{TP+TN+FP+FN} \in \left[0,1\right]
    $$

    La principal ventaja de esta métrica es que proporciona una medida simple y fácilmente interpretable de qué tan bien el modelo está clasificando las instancias.

    Sin embargo, cuando las clases en el conjunto de datos están desbalanceadas, es decir, hay muchas más instancias en una clase que en otras, o cuando el coste de fallar cada clase es distinto, la \textit{accuracy} puede ser engañosa. Un modelo que predice siempre la clase mayoritaria puede tener una alta \textit{accuracy} simplemente debido al desbalance de clases, pero este modelo no sería útil en la práctica.

    % ------------------------------------------------------------------------------------------------------ %

    \item \textbf{Precisión}: Es la proporción de verdaderos positivos (TP) respecto a la suma de verdaderos positivos y falsos positivos (FP).
     
    $$
    Precision = \frac{TP}{TP+FP} \in \left[0,1\right]
    $$

    Responde a la pregunta "¿Qué tan efectivo es el modelo en identificar la mayoría de los casos positivos reales?".

    % ------------------------------------------------------------------------------------------------------ %

    \item \textbf{Exhaustividad (\textit{recall})}: Es la proporción de verdaderos positivos (TP) respecto a la suma de verdaderos positivos y falsos negativos (FN).
     
    $$
    Recall = \frac{TP}{TP+FN} \in \left[0,1\right]
    $$

    Responde a la pregunta "¿Qué tan efectivo es el modelo en identificar la mayoría de los casos positivos reales?".

    % ------------------------------------------------------------------------------------------------------ %
    
    \item \textbf{F1-Score}: Se considera como una media armónica que combina los valores de la precisión y de la exhaustividad. Su objetivo es medir la calidad de las predicciones dentro de la clase positiva. 

    De esta forma, la métrica F1 se calcularía:
    $$
    \textnormal{F1-Score} = \frac{2 \times Precision \times Recall}{Precision+Recall} \in \left[0,1\right]
    $$

    Esta métrica es especialmente útil cuando se tienen conjuntos de datos desbalanceados, donde hay muchas más instancias de una clase que de otra. 

    Por contraparte, proporciona una medida combinada de la precisión y el \textit{recall}, que puede no ser tan intuitiva de interpretar como otras métricas más simples, como la \textit{accuracy}.

    % ------------------------------------------------------------------------------------------------------ %
    
    \item \textbf{G-Mean}: Mide qué tan bien el modelo clasifica ambas clases, a partir de dos medidas:
    
    \begin{itemize}
        \item La sensibilidad (\textit{sensitivity}), que es el \textit{recall} de la clase positiva.
        \item La especifidad (\textit{specifity}), que es el \textit{recall} de la clase negativa. 
    \end{itemize}

    De esta forma, la métrica G-Mean se calcularía:
    $$
    \textnormal{G-Mean} = \sqrt{Sensitivity \times Specifity} \in \left[0,1\right]
    $$

    A diferencia de F1-Score, esta no le da más importancia a un tipo de error sobre otro. Es por ello que es útil en escenarios donde se busca un rendimiento equilibrado entre ambas clases, como en problemas con clases desbalanceadas, ya que penaliza los modelos que presentan un rendimiento muy desigual entre clases. 

    Un valor alto de G-Mean indica que el modelo mantiene un buen equilibrio entre la detección de instancias positivas y negativas, mientras que un valor bajo sugiere que el modelo tiende a favorecer una de las clases en detrimento de la otra.

    % ------------------------------------------------------------------------------------------------------ %

    \item \textbf{La curva ROC y la AUC}: La curva ROC proporciona una representación gráfica de la relación entre la tasa de verdaderos positivos (TPR) y la tasa de falsos positivos (FPR) a diferentes umbrales de decisión del modelo.

    La naturaleza de los umbrales de decisión depende en gran medida del modelo de clasificación utilizado y de la forma en que este modelo produce sus predicciones.

    Por ejemplo, en el \textit{k}-NN los umbrales de decisión dependerán del valor de $k$, puesto que uno pequeño puede ser muy sensible a instancias individuales, que se comporte de manera errática, en cambio, un valor alto suaviza las decisiones, promediando la influencia de los vecinos, todavía más si se pondera el peso de la decisión con la distancia a la que está cada una de las $k$ instancias.

    En el \textit{Random Forest} los umbrales de decisión dependerán de la votación combinada de los árboles individuales. Los umbrales de decisión pueden ajustarse al modificar la probabilidad mínima que se necesita para clasificar una instancia en una clase específica. Al incrementar el umbral, se requiere un consenso más fuerte entre los árboles para clasificar una instancia en una clase positiva, lo cual reduce la tasa de falsos positivos (FPR), pero también puede reducir la tasa de verdaderos positivos (TPR).

    Esta variabilidad de la curva dependiente de los umbrales de decisión y la naturaleza de cada modelo de clasificación hace que debamos realizar la comparación con precaución, y salvando las distancias.

\end{itemize}

% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %

\subsection{Experimentación}

\subsubsection{Definición del protocolo de validación experimental}

A continuación, procederemos a entrenar y evaluar distintos modelos de \textit{machine learning} para los datos del problema, seleccionando finalmente el modelo con el mejor rendimiento. Es fundamental evitar el \textit{data snooping} durante este proceso, para asegurar que la evaluación refleja un rendimiento realista y generalizable.

El \textit{\textbf{data snooping}} ocurre cuando la información de los datos de test dirige el entrenamiento del modelo, es decir, cuando usamos repetidamente los datos de test para tomar decisiones sobre el modelo, como escoger hiperparámetros, seleccionar el mejor modelo de varios probados, o ajustar variables. Como resultado,el modelo se ajusta no solo a los datos de entrenamiento, sino también a las particularidades de los datos de test, llevando a una posible generalización deficiente.

Esta mala práctica podemos evitarla siguiendo un enfoque riguroso en la gestión de los conjuntos de datos.Para lograrlo deberemos:

\begin{itemize}

    \item \textbf{Dividir los datos en conjuntos de entrenamiento, validación y test}: El conjunto de entrenamiento se utiliza para ajustar los parámetros del modelo; el conjunto de validación sirve para la selección de hiperparámetros y la comparación de modelos; y el conjunto de test solo se usa al final, exclusivamente para evaluar el rendimiento del modelo elegido. De esta manera, los datos de test no influyen en ninguna decisión de desarrollo.

    \item \textbf{Aplicar validación cruzada}: Esta técnica permite obtener una evaluación más robusta y confiable del rendimiento del modelo utilizando solo los datos de entrenamiento y validación. Al realizar múltiples divisiones de los datos de entrenamiento, la validación cruzada ayuda a reducir la dependencia de un solo conjunto de validación, minimizando el riesgo de sobreajuste.

    Una implementación de validación cruzada es el \textit{k-fold}, en la que el conjunto de datos se divide en $k$ partes (o \textit{folds}) de tamaño similar. El modelo se entrena $k$ veces, usando en cada iteración $k-1$ pliegues para entrenar y el pliegue restante para validar. 

    En este problema, dado que tenemos un gran número de instancias, utilizaré $k=5$ para la validación cuzada.
    
    \item \textbf{Evitar la revisión o análisis del conjunto de test}: Es crucial no utilizar los datos de test para hacer ajustes adicionales en el modelo. El conjunto de test debe mantenerse separado y solo emplearse para la evaluación final del modelo, después de haber tomado todas las decisiones de desarrollo.
    
\end{itemize}

% ---------------------------------------------------------------------------------------------------------- %

\subsubsection{Optimización de hiperparámetros}

Primero, se plantea la optimización de hiperparámetros de los distintos modelos que hemos propuesto, mediante los nodos <<Parameter Optimization Loop>> de la extensión \textit{KNIME Optimization extension}. En la Figura \ref{fig:churn_spotify_hyperparameter_optimization_knn} se visualiza el esquema de nodos para la optimización del hiperparámetro $k$ del modelo \textit{k}-nearest neighbors. Se ha probado con valores enteros de $k$ entre 1 y 20, y se maximiza la métrica G-Mean (véase la Figura \ref{fig:churn_spotify_config_loop_opt}). 
Esta optimización de parámetros se ha probado con división de los datos de entrenamiento, con el objetivo de evaluar el desempeño del modelo de manera más robusta. Los resultados (véase la Figura \ref{fig:churn_spotify_opt_results}) en este caso muestran que, a menor $k$, mejor es el desempeño del modelo. Por tanto, el modelo \textit{k}-NN candidato tendrá $k=1$.

\begin{tcolorbox}
    [colback=gray!5!white, colframe=gray!60!black, title=NOTA, breakable]

    Se ha escogido G-Mean en vez de F1-Score, dado que varios modelos no clasificaban ninguna instancia como \code{CHURNED}, lo que daba F1-Score indeterminado.

\end{tcolorbox}


\begin{figure}[h!]
    \fbox{\includegraphics[width=1.0\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_hyperparameter_optimization.png}}
    \caption{
        Esquema de optimización para el modelo \textit{k}-NN.
    }
    \label{fig:churn_spotify_hyperparameter_optimization_knn}
\end{figure}

\begin{figure}[h!]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{secciones/sec2/imagenes/churn_spotify_config_start_loop_opt.png}
        \caption{Configuración del nodo inicial del bucle de optimización.}
        \label{fig:churn_spotify_config_start_loop_opt}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.5\textwidth}
        \centering
        \includegraphics[width=\textwidth]{secciones/sec2/imagenes/churn_spotify_config_end_loop_opt.png}
        \caption{Configuración del nodo final del bucle de optimización.}
        \label{fig:churn_spotify_config_end_loop_opt}
    \end{subfigure}
    \caption{Configuración del bucle de optimización para el modelo \textit{k}-NN.}
    \label{fig:churn_spotify_config_loop_opt}
\end{figure}

\begin{figure}[h!]
    \centering
    \fbox{\includegraphics[width=0.5\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_opt_results.png}}
    \caption{
        Resultados de la optimización para el modelo \textit{k}-NN.
    }
    \label{fig:churn_spotify_opt_results}
\end{figure}

\begin{tcolorbox}
    [colback=gray!5!white, colframe=gray!60!black, title=Técnicas de optimización, breakable]

    Existen numerosas técnicas de optimización de parámetros. Las dos técnicas más simples son:

    \begin{itemize}
    
        \item \textbf{Búsqueda exhaustiva o en \textit{grid}}: Consiste en definir un conjunto finito de valores posibles para cada hiperparámetro y evaluar el modelo con todas las combinaciones posibles de dichos valores. Esta técnica garantiza encontrar la mejor combinación dentro del espacio definido, pero puede resultar muy costosa computacionalmente cuando el número de parámetros o el tamaño del rango de búsqueda es elevado.
        
        \item \textbf{Búsqueda aleatoria (\textit{random search})}: En lugar de probar todas las combinaciones posibles de hiperparámetros, selecciona al azar un número determinado de combinaciones dentro de los rangos definidos por el usuario. Aunque no explora sistemáticamente todo el espacio de búsqueda, suele ser más eficiente en tiempo y, en muchos casos, alcanza resultados comparables a la búsqueda exhaustiva.
    
    \end{itemize}

\end{tcolorbox}

Al igual que con este modelo, se ha realizado optimización en el resto:

\begin{itemize}
    
    \item \textit{Support Vector Machine}:
    Se experimentó con distintos \textit{kernel} lineal y polinomial, valores del parámetro de coste $C$ comprendidos entre $2^{-5}$ y $2^{10}$, y grados ---para determinar entre lineal o polinomial--- entre 1 y 5. Dado el elevado número de combinaciones posibles, se optó por una estrategia de random search con 40 iteraciones. La configuración óptima obtenida ha sido con \textit{kernel} polinómico de grado $3$ y con coste $C$ de $2^{8}$.

    \item \textit{Decision Tree}:
    Se probaron configuraciones con y sin poda, y variando el número mínimo de instancias por nodo interno entre $2^1$ y $2^5$. En este caso, fue factible realizar una búsqueda exhaustiva sobre todas las combinaciones posibles. La configuración óptima encontrada correspondió a un árbol sin poda y con un mínimo de 2 instancias por nodo.

    \item \textit{Random Forest}:
    Se evaluaron combinaciones del número de árboles entre 50 y 500, profundidad máxima del árbol entre $2^1$ y $2^{10}$, y tamaño mínimo del nodo entre $2^1$ y $2^{10}$. Se realizó búsqueda aleatoria de las 3 variables durante 40 iteraciones. La configuración óptima obtenida fue de 420 árboles con una profundidad máxima de 512 y 2 instancias por nodo mínimas.

    \item \textit{Gradient Boosting}:
    Se analizaron valores del número de árboles entre 50 y 500, profundidades de árbol entre $2^1$ y $2^5$ y, y tasas de aprendizaje (\textit{learning rate}) comprendidas entre $4^{-5}$ y $4^0$. En este caso, se empleó una estrategia de \textit{random search} con 40 iteraciones. La configuración óptima hallada fue: 131 árboles, profundidad máxima de 512 y \textit{learning rate} de $4^{-5} \approx 0.001$.

\end{itemize}


\subsubsection{Comparación de modelos candidatos}

Finalmente, se prueba a entrenar cada modelo con los hiperparámetros obtenidos, realizando validación cruzada tal y como se especificó anteriormente. Los resultados, plasmados en la Figura \ref{fig:churn_spotify_comparison_results} reflejan dos comportamiento: 

\begin{itemize}
    
    \item Los modelos SVM, \textit{Decision Tree} y \textit{Random Forest} presentan un \textit{recall} inferior al 5\%, que llega a un 0\% en el caso del árbol de decisión. Esto nos lleva a pensar que apenas clasifican instancias como \code{CHURNED}, limitándose prácticamente a predecir la clase mayoritaria. En consecuencia, el modelo \textit{Decision Tree} obtiene un F1-Score indeterminado, ya que no realiza ninguna predicción positiva, lo que provoca una división por cero en el cálculo de dicha métrica.
    
    \item Los modelos \textit{k}-NN y \textit{Gradient Boosting}, en cambio, presenta un \textit{recall} ligeramente superior, sospechosamente cercano al 25.9\% de instancias de la clase \code{CHURNED}, lo que sugiere que la clasificación es aleatoria y que el modelo podría estar simplemente asignando etiquetas positivas al azar en una proporción similar a la distribución original de clases.

\end{itemize}

Estos resultados se refuerzan con lo que se ve en la Figura \ref{fig:churn_spotify_ROC_comparison}, donde las curvas ROC de los distintos modelos se sitúan en torno a la recta aleatoria ---y, por tanto, los valores AUC aproximan a 0.5---, evidenciando una capacidad discriminativa muy limitada. En otras palabras, los modelos no logran distinguir de manera efectiva entre las clases \code{CHURNED} y \code{NOT\_CHURNED}, mostrando un comportamiento próximo al de una clasificación aleatoria.

\begin{figure}[h!]
    \centering
    \fbox{\includegraphics[width=\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_models_comparison.png}}
    \caption{
        Métricas de desempeño logradas por los modelos candidatos.    
    }
    \label{fig:churn_spotify_comparison_results}
\end{figure}

\begin{figure}[h!]
    \centering
    \fbox{\includegraphics[width=\textwidth, keepaspectratio]{secciones/sec2/imagenes/churn_spotify_ROC_comparison.png}}
    \caption{
        Curvas ROC de los modelos candidatos.
    }
    \label{fig:churn_spotify_ROC_comparison}
\end{figure}

% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %

\subsubsection{Modelo final}

No se ha escogido ningún modelo, ya que se desestima su uso al no alcanzar un rendimiento aceptable en las métricas de evaluación, mostrando un comportamiento cercano al azar.

% El modelo final escogido es, por tanto, \textit{Gradient Boosting}, con la configuración óptima para los  hiperparámetros obtenida anteriormente: 249 árboles, profundidad máxima de 26 y 

% valores óptimos obtenidos anteriormente para los hiperparámetros. El modelo logra un F1-Score de apenas 0.29 y un G-Mean de 0.469. Estos son valores muy pobres, ya que un modelo que clasifica aleatoriamente ---si bien, en un escenario balanceado--- típicamente obtendría un F1-Score y un G-Mean de 0.5. El bajo valor del F1-Score indica una deficiente capacidad para identificar correctamente los casos positivos, mientras que el G-Mean confirma que el modelo no logra equilibrar adecuadamente el rendimiento entre ambas clases.

% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %
% ---------------------------------------------------------------------------------------------------------- %

\subsection{Conclusión}

Los resultados obtenidos ponen de manifiesto la dificultad de predecir la cancelación de suscripciones (\textit{churn}) en el conjunto de datos analizado. A pesar de haberse realizado una optimización exhaustiva de hiperparámetros y validación cruzada para cada modelo, ninguno ha alcanzado un rendimiento satisfactorio. Las métricas de evaluación ---especialmente el \textit{recall}, el F1-Score y el G-Mean--- revelan una escasa capacidad de discriminación entre las clases, situándose los modelos cerca del comportamiento aleatorio.

En particular, los modelos \textit{Decision Tree}, \textit{Random Forest} y SVM tienden a predecir únicamente la clase mayoritaria, mientras que \textit{k}-NN y \textit{Gradient Boosting} muestran ligeras mejoras, aunque insuficientes para considerarse útiles en la práctica.

Estos resultados sugieren la necesidad de incorporar nuevas variables que aporten mayor capacidad discriminativa. 


